# Configuration Railway - Serveur WebSocket + Bot Telegram intégré
# Un seul service : WebSocket + alertes parties en ligne

[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "npm run server"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
