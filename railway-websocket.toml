# Configuration Railway pour le serveur WebSocket (second service)
# Utiliser ce fichier pour le service WebSocket uniquement.

[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "npm run server"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
